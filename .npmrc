# react-helmet-async@2 deklariert nur React 16-18 als Peer, läuft aber problemlos mit React 19.
# Ohne dieses Flag schlägt jedes frische `npm install` (z. B. Vercel, CI) mit ERESOLVE fehl.
legacy-peer-deps=true
